/**
 * Grid Coordinator Types Re-export
 *
 * Re-exports types from the main coordinator module for the Grid view.
 *
 * @see apps/spreadsheet/src/coordinator/types.ts
 */

export type {
  RendererDependencies,
  SheetCoordinatorConfig,
  SheetStateProvider,
} from '../../../coordinator';
