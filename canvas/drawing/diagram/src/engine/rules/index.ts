/**
 * Rule Engine
 *
 * The adaptive rule engine that adjusts constraint values when
 * content doesn't fit within the initial constraint resolution.
 *
 * @module rules
 */

export { applyRules } from './rule-engine';
export type { RuleEngineInput, RuleEngineOutput } from './rule-engine';
