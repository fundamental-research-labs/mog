[package]
name = "compute-layout-index"
version = "0.1.0"
publish = false
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Sparse pixel layout: O(k) dimension prefixes for on-demand cell-to-pixel mapping"

[dependencies]
domain-types = { path = "../../../../domain-types", default-features = false }
thiserror = { workspace = true }

[dev-dependencies]
proptest = "1"
