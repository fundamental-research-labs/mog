[package]
name = "pivot-types"
version = "0.1.0"
publish = false
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Pivot table type definitions — shared between compute-pivot, domain-types, and xlsx-parser"

[dependencies]
value-types = { path = "../value-types" }
cell-types = { path = "../cell-types" }
domain-types = { path = "../../../../../domain-types", default-features = false }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
bridge-types = { path = "../../../../../infra/rust-bridge/bridge-types" }

[dev-dependencies]
# Drives the `tests/no_bare_f64_at_boundary.rs` build-time gate.
finite-at-boundary-walker = { path = "../finite-at-boundary-walker" }
