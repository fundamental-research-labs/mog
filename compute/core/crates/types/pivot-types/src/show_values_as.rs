//! **NOTE**: Canonical definitions now live in `domain_types::domain::pivot::show_values_as`.
//! This module re-exports for backward compatibility.

pub use domain_types::domain::pivot::{
    RelativePosition, ShowValuesAs, ShowValuesAsBaseItem, ShowValuesAsConfig, SortByValueConfig,
};
