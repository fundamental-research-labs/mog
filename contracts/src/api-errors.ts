/**
 * Re-export shim. Source lives in @mog/types-api (types/api/src/api/api-errors.ts).
 * Serves the `@mog-sdk/contracts/api-errors` subpath for external
 * consumers.
 */
export type * from '@mog/types-api/api/api-errors';
