/**
 * Views Module
 *
 * Shared types for the multi-view system (grid, kanban, timeline, etc.).
 *
 * @module @mog-sdk/contracts/views
 */

export type {
  ColumnSchema,
  ColumnTypeKind,
  EditTarget,
  IViewAdapter,
  IViewRegistry,
  SelectOption,
  TableId,
  ToolbarContext,
  Unsubscribe,
  ViewId,
  ViewSelection,
  ViewType,
} from './types';
