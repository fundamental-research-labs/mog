# Spreadsheet Dev App

This is the public Vite host for developing and manually testing the Mog
spreadsheet app.

From the public repo root:

```bash
pnpm dev
```

Or from this directory:

```bash
pnpm dev
```

The dev server listens on `http://localhost:3002` by default.
