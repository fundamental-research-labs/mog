import { cellId, colId, rowId, toCellId, toColId, toRowId } from '@mog-sdk/contracts/cell-identity';

const fixtureCellId = toCellId('cell-fixture');
const fixtureRowId = toRowId('row-fixture');
const fixtureColId = toColId('col-fixture');

void fixtureCellId;
void fixtureRowId;
void fixtureColId;
void cellId;
void rowId;
void colId;
