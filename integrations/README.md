# Mog Integrations

This directory contains packages where Mog runs inside another host or platform.

- `integrations/`: Mog embedded into external products such as VS Code.
- `extensions/`: reserved for packages that extend Mog itself.
