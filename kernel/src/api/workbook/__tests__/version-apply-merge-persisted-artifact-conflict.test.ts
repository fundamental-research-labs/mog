import { registerPersistedConflictArtifactScenario } from './version-apply-merge-persisted-artifact-conflict-scenario';

describe('WorkbookVersion persisted conflicted merge preview artifacts', () => {
  registerPersistedConflictArtifactScenario();
});
