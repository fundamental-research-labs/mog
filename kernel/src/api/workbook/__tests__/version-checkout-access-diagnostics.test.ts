import { describe } from '@jest/globals';

import { registerCheckoutAccessDiagnosticsScenarios } from './version-checkout-access-diagnostics-scenarios';

describe('WorkbookVersion checkout access diagnostics', () => {
  registerCheckoutAccessDiagnosticsScenarios();
});
