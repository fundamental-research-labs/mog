import { registerReviewProviderApprovalScenarios } from './version-review-provider-approval-scenarios';

describe('WorkbookVersion provider-backed review approval', () => {
  registerReviewProviderApprovalScenarios();
});
