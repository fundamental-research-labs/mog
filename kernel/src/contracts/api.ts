/**
 * Compatibility export for API contract consumers.
 *
 * The canonical source lives in @mog-sdk/contracts/api; this
 * source entrypoint exists so @mog-sdk/kernel/contracts/api has a real
 * package-export source path and source-emitted declaration output.
 */
export * from '@mog-sdk/contracts/api';
