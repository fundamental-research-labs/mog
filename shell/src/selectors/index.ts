/**
 * Shell Selectors Barrel Export
 *
 * Local selector copies for the shell package (migrated from
 * @mog-sdk/kernel/selectors during kernel export tightening).
 */

export { focusSelectors } from './focus';
export type { FocusState } from './focus';
