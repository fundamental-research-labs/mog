/**
 * Extensions Module
 *
 * Shared types for the extension/add-in system.
 *
 * @module @mog-sdk/contracts/extensions
 */

export type {
  ExtensionAuthor,
  ExtensionCSPConfig,
  ExtensionInstance,
  ExtensionLifecycleState,
  ExtensionManifest,
  ExtensionOfficeJsConfig,
  ExtensionPanelConfig,
  ExtensionPermission,
} from './types';
