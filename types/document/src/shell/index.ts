/**
 * @mog-sdk/types-document/shell — Shell-level service contracts.
 *
 * See `./types.ts` for the `ShellService` capability interface.
 */
export type { LoadDocumentOptions, ShellDocumentState, ShellService } from './types';
