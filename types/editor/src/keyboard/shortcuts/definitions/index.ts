/**
 * Keyboard Shortcut Definition Types
 *
 * This module re-exports the KeyboardShortcut type for consumers that need it.
 *
 * For runtime shortcut definitions (KEYBOARD_SHORTCUTS array, category arrays),
 * import from the spreadsheet app: apps/spreadsheet/src/keyboard/definitions/
 */

// Re-export types for convenience
export type { KeyboardShortcut } from '../types';
