/**
 * Selectors Barrel Export
 *
 * Exports selector functions for querying floating objects from sheet data.
 * These are read-only utilities that operate on deserialized sheet snapshots.
 */

export * from './diagram';
